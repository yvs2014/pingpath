
Basic Build
------------------

It can be built with make or cmake (preferable)

1) make
make
#sudo make install

2) cmake
cmake -B _build -S $(pwd)
cmake --build _build
#sudo cmake --install _build --strip

Package Templates
------------------

Templates are located in
debian/
rpmlinux/
archlinux/
snap/

